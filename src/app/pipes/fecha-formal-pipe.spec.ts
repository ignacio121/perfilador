import { FechaFormalPipe } from './fecha-formal-pipe';

describe('FechaFormalPipe', () => {
  it('create an instance', () => {
    const pipe = new FechaFormalPipe();
    expect(pipe).toBeTruthy();
  });
});
